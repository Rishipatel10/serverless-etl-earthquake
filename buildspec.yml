version: 0.2

phases:
  build:
    commands:
      - echo "Validating Lambda build syntax..."
      - pip install -r requirements.txt || true
      - python -m py_compile lambda_function.py
      - echo "Build completed successfully"
artifacts:
  files:
    - lambda_function.py
    - requirements.txt